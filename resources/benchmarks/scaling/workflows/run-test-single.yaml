name: test-scaling-single-node-jobs
description: deploy 500 single-replica jobs
tasks:
- id: job
  type: SubmitObj
  params:
    refTaskId: register
    count: 500
    params:
      replicas: 1
      ttl: 2m
