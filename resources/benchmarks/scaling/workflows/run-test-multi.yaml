name: test-scaling-multi-node-job
description: deploy a 500-replicas job
tasks:
- id: job
  type: SubmitObj
  params:
    refTaskId: register
    count: 1
    params:
      replicas: 500
      ttl: 2m
